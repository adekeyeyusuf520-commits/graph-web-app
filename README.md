# Graph Web App

A simple Flask + Chart.js web app that lets users switch between different graph types (line, bar, pie).

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
